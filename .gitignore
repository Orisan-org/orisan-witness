node_modules/
dist/
*.tsbuildinfo
.DS_Store
data/
*.db
*.db-*

# Secrets. This used to pin only the literal filename witness-signing.key;
# a glob covers any .env/.pem/.key file dropped anywhere in the tree, not
# just that one name.
.env
.env.*
*.pem
*.key
